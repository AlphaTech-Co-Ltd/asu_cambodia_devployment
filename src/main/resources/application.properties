# ------------------------------
# Application
# ------------------------------
spring.application.name=asu_cambodiabackend
server.port=${PORT:8080}
jwt.secret=zstK6s4dDS/5kOA4RuRJXDbXcclD1UbwLG4fCu8XKIg=

# ------------------------------
# Database Configuration (Fixed)
# ------------------------------
# Use ONE of these options:

# Option A: Use Railway's DATABASE_URL directly (recommended)
spring.datasource.url=${DATABASE_PUBLIC_URL}

# Option B: Use individual components (alternative)
# spring.datasource.url=jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}

spring.datasource.username=${PGUSER}
spring.datasource.password=${PGPASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection pool settings (critical)
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.idle-timeout=10000

# ------------------------------
# JPA/Hibernate
# ------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Remove this line (let Hibernate auto-detect)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ------------------------------
# Debugging (temporary)
# ------------------------------
logging.level.org.springframework.boot.autoconfigure=WARN
logging.level.org.springframework.context=WARN

# Keep important logs visible
logging.level.org.hibernate.SQL=ERROR
logging.level.org.springframework.web=INFO
logging.level.com.yourpackage=DEBUG